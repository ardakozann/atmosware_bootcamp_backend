package com.turkcell;

public class Sunum {
	// İnterpreter compiler nedir ?
	// Java interpreter ? complier ? -- Javada interpreter vardır. complier
	// davardır.
	// JavaScript interpreter ? complier ? interpreter => yorumlayıcı
	// open source nedir ?
	// Jdk JRE JVM nedir ?
	// JIT nedir ?
	// Java 100% OOP ? (içerisinde (8 adet) primitive türler olduğu için 100% oop
	// değildir.
	// Java By Pass Value ? By Pass Referances ?
	// Java 8 ile gelen özellikler nelerdir ?
	// primitive types - wrapper class arasındaki farklar ?
	// stack memory heap memory arasındaki farklar ?
	/// ascii - unicode
	
	// JVM + = JRE
	// JRE +
	// Yüksek seviyedeki dili (JAVA, C#, Pyhton) gibi dillerin makine kodlarına
	// çevrilmesi
	
	// deneme.java -> complier -> deneme.class -> linux, unix
	
	// -------------------------------------------------------------------
	// Scanner da close komutu neden kullanılır ?
	
	// -------------------------------------------------------------------
	
	// valueOf() (obje olarak çalışır) (toString parseInt obje mi bak) -->
	// toString(), parseInt() aralarındaki farklar nelerdir ?
	// String concat(), + operant, StringBuilder aralarındaki ilişki nedir ?
	// StringBuilder ve StringBuffer farkı (Race condition neydi bak)
	// regex: regular expression nedir ?
	// --------------------------------------------------------------------
	// Java serileştirme seriliablization
	// decoder encoder nedir ?
	
	// ------------------------
	// String cozulmus = ""; String cozulmus = new String(); farkı
	// SHA yapısı MD5
}
