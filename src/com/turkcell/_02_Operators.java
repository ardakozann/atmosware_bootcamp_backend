package com.turkcell;

public class _02_Operators {
	
	// aritmetic operators: + - * / %
	// relation operators : > >= < <= ==
	// logic operatorrs : && (VE) || (VEYA) ! (DEĞİL)
	
	// x=x+1
	// x+=1
	// x++
}
